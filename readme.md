# Product & Order Microservice

A simple Node.js microservice for managing **Products** and **Orders**, secured with **JWT authentication** and documented with **Swagger UI**.

---

## 🚀 Features
- REST API built with **Express**
- **JWT authentication** for protected endpoints
- **Swagger UI** (`/api-docs`) for interactive API exploration
- Data persisted in local JSON files (`products.json`, `orders.json`)
- Modular route structure (`routes/products.js`, `routes/orders.js`)

---

## 📂 Project Structure
---

## ⚙️ Setup

1. Clone the repository:
   ```bash
   git clone https://github.com/<your-username>/product-order-service.git
   cd product-order-service